{% extends 'base.html.twig' %}

{% block title %}ECFJ{% endblock %}

{% block body %}
<div class="card mt-4" style="width: 18rem;">
    <div class="card-body">
        {% for article in articles %}
            <h5 class="card-title text-center">{{ article.nom }}</h5>
            <h6 class="card-subtitle mb-2 text-center">{{ article.marque.nom }}</h6>
            <p class="card-text text-center">Reste {{ article.quantite }} en stock.</p>
            <a href="{{ path('detail_article') }}" class="card-link">Détail de l'article</a></br>
            <a href="{{ path('add_article') }}" class="card-link">Editer un article</a>
        {% endfor %}
    </div>






</div>


{% endblock %}
